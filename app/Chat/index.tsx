import { Button, Group, Stack, Title, Text } from '@mantine/core';
import { useMediaQuery } from '@mantine/hooks';
import { theme } from '../../components/config/theme';

export function Chat() {
  return <Text>This is our chat page</Text>;
}
